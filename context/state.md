# state

- **In repo:** Rust `paper_bot` demo (`cargo run --bin paper_bot`); HTTP `server` for `POST /v1/evaluate`.
- **Last change:** Removed Python-based HTTP demo and historical walk-forward docs from this repo.
- **2026-04-18 — Backtest table follow-up:** Baseline ~40% win + PF ~1.14 matches breakeven math for 2×ATR stop / 3×ATR target (~40% needed). Engine behavior aligns with spec; edge is ~zero before costs. `no_runway` gates clearance with `stop_atr_multiple` not `target_atr_multiple` (possible design bug). See `memory.txt`.
- **2026-04-18 — Strategy basis:** `context/strategy-basis.md` (bullish 15m continuation pullback; EMA/VWMA/CVD/ATR + vetoes). Full narrative in `README.md` “Theory of the engine”.
- **2026-04-18 — Volume profile:** Rolling VP (POC/VAL/VAH) in `src/indicators/volume_profile.rs`; `PreparedCandle` fields `vp_*`; gate `below_volume_profile_val` when `vp_enabled` (default true in `StrategyConfig::default`, fixtures use `vp_enabled: false`).
- **2026-04-18 — Indicators + strategies layout:** `src/indicators/` = one file per TA formula + `mod.rs`; `src/market_data/` = `PreparedCandle`/`PreparedDataset` + `IndicatorSnapshot` (RSI, MACD, BB, stoch, SMAs, OBV, ADX, CCI, Williams %R); `src/strategies/default/` = named **default** continuation strategy (`strategy_id` `"default"`); `src/strategy/` = shared `SignalDecision`, formulas, `FailedAcceptanceState`. `DecisionMachine` builds dataset then `strategy_engine_for(&config)`.
- **2026-04-18 — More indicators:** WMA, ROC(10), MFI(14), Keltner(20/14/2), Donchian(20), Aroon(25), Hull(9), Ultimate Oscillator, TSI(25,13); fields on `IndicatorSnapshot` sub-structs; still not used by default strategy gates.
- **2026-04-18 — Full missing-TV plan:** See [`context/indicator-roadmap.md`](indicator-roadmap.md) (tiers 1–9: A/D, CMF, volume MA/EMA, BB%B/bandwidth, Stoch RSI, AO, PSAR, SuperTrend, VWAP+anchor, Ichimoku, pivots, DEMA/TEMA, etc.).
- **2026-04-18 — Roadmap implemented:** Tiers 1–9 in `src/indicators/*.rs`; `IndicatorSnapshot` + `prepare.rs` wiring; `StrategyConfig` gained `vwap_anchor_mode` (`UtcDay` / `RollingBars` / `Disabled`), `vwap_rolling_bars`, `vwap_include_current_bar` (serde-defaulted). `cargo test` green; default strategy unchanged.
