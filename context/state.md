# state

- **2026-04-19 — README top:** Added **In plain language** (what it is / what you get / what you do not get) above the one-sentence technical blurb; points readers to the rest of the doc for depth.
- **2026-04-19 — perf / scale (HTTP evaluate):** Stateless `POST` body + read-only `Arc<DecisionMachine>` → horizontal replicas OK. `EVALUATE_MAX_INFLIGHT` **optional** (unset = no software inflight cap; set `>0` = `ConcurrencyLimitLayer` per process). `DecisionMachine::evaluate` moves request fields; last bar `pop()` not `clone()`; capabilities in `Arc`. Release profile LTO/strip. `capabilities()` → `&MachineCapabilities`.
- **2026-04-19 — README readability:** Top “one sentence” blurb, **Quick navigation** table, **What this is / is not**, clearer HTTP env + JSON errors, **Paper bot** “use when”, **Architecture** table aligned to real `src/` layout (removed stale `strategy/gates` paths).
- **2026-04-19 — rust-strict-build S2 full:** `clippy` with `-D` correctness..cargo **+ pedantic + nursery** green. Scoped `#![allow(clippy::pedantic, clippy::nursery)]` on module roots (`indicators`, `statistics`, `strategies`, `strategy`, `market_data`, `config`, `domain`, `machine`, `context`) + integration tests `indicators_all` / `system_mock_input` with one-line rationales. `paper_bot`: doc linebreak, `mul_add`, `#[allow(too_many_lines)]` on `main`. `server`: `match`→`if let` for `single_match_else`.
- **2026-04-19 — CI-style verify (practical clippy):** `cargo fmt --check`; `RUSTFLAGS=-Dwarnings` `cargo check` + `check --release` (`--all-targets --locked`); clippy `-D` groups correctness+suspicious+style+complexity+perf+cargo (**not** pedantic/nursery — those hit ~300+ across indicators); `cargo test --locked`; `RUSTDOCFLAGS=-Dwarnings` `cargo doc --no-deps --document-private-items`; `cargo audit` clean. Fixes: `strategy_id_tests` struct update; `strategies/mod.rs` rustdoc redundant link; `server.rs` `collapsible_if`.
- **2026-04-19 — server hardening:** `EVALUATE_API_KEY` optional (if set: `X-Api-Key` or `Authorization: Bearer` on `POST /v1/evaluate` only). `HOST` env (`IpAddr`, default `0.0.0.0`). `DefaultBodyLimit` 10MiB on router. Client errors: JSON `invalid_request` / `unauthorized` (details in logs only). `.gitignore`: `.env`, `.env.*`, `!.env.example`.
- **2026-04-19 — rust-vuln-intel pass:** `cargo audit` clean (1049 advisories in DB, 196 lockfile deps). Stack: `axum` 0.8.9, `hyper` 1.9.0, `reqwest` 0.12.28, `rustls` 0.23.38, etc. RUSTSEC-2022-0055 **N/A** (modern axum).
- **2026-04-18 — Extra indicator strategies:** `strategy_engine_for` accepts `macd_trend`, `rsi_pullback`, `supertrend_adx`, `bb_mean_reversion`, `stoch_crossover`, `ichimoku_trend`, `ttm_squeeze_fire`, `donchian_breakout` (+ files under `src/strategies/`); shared vetoes in `safety.rs`. HTTP: `config_overrides.strategy_id`.
- **2026-04-18 — optimize rule pass:** `cargo audit` clean (no RUSTSEC in output). Root `Cargo.toml` has no `[profile.release]` / `[profile.dev]` (defaults only). Dominant cost per `POST /v1/evaluate`: `PreparedDataset::build` (full indicator library + per-bar VP); secondary: `latest_frame` full `PreparedCandle` clone for JSON diagnostics. See chat for tiered findings; no code changes applied in this pass.
- **2026-04-18 — rust-strict-build (core):** `cargo fmt --check`; `RUSTFLAGS=-Dwarnings cargo check --all-targets --locked`; `cargo clippy --all-targets --locked` with `-D` on clippy groups **correctness, suspicious, style, complexity, perf, cargo** (not pedantic/nursery). Fixes: `stochastic.rs` `%K` `%D` window uses `.iter().flatten()` (equivalent to skip-`None` + `cnt == d_period`); `server.rs` env override `let` chain + `map_err(ApiError)`; `paper_bot.rs` collapsed `if`/`let` chains for stop + arm-long.
- **In repo:** Rust `paper_bot` demo (`cargo run --bin paper_bot`); HTTP `server` for `POST /v1/evaluate`.
- **2026-04-19 — Indicator smoke tests:** `tests/indicators_all.rs` runs `PreparedDataset::build` on 260 synthetic 15m candles + asserts last-bar `Option<f64>` fields are finite when `Some` (full `prepare.rs` indicator path).
- **Last change:** Removed Python-based HTTP demo and historical walk-forward docs from this repo.
- **2026-04-18 — Backtest table follow-up:** Baseline ~40% win + PF ~1.14 matches breakeven math for 2×ATR stop / 3×ATR target (~40% needed). Engine behavior aligns with spec; edge is ~zero before costs. `no_runway` gates clearance with `stop_atr_multiple` not `target_atr_multiple` (possible design bug). See `memory.txt`.
- **2026-04-18 — Strategy basis:** `context/strategy-basis.md` (bullish 15m continuation pullback; EMA/VWMA/CVD/ATR + vetoes). Full narrative in `README.md` “Theory of the engine”.
- **2026-04-18 — Volume profile:** Rolling VP (POC/VAL/VAH) in `src/indicators/volume_profile.rs`; `PreparedCandle` fields `vp_*`; gate `below_volume_profile_val` when `vp_enabled` (default true in `StrategyConfig::default`, fixtures use `vp_enabled: false`).
- **2026-04-18 — Indicators + strategies layout:** `src/indicators/` = one file per TA formula + `mod.rs`; `src/market_data/` = `PreparedCandle`/`PreparedDataset` + `IndicatorSnapshot` (RSI, MACD, BB, stoch, SMAs, OBV, ADX, CCI, Williams %R); `src/strategies/default/` = named **default** continuation strategy (`strategy_id` `"default"`); `src/strategy/` = shared `SignalDecision`, formulas, `FailedAcceptanceState`. `DecisionMachine` builds dataset then `strategy_engine_for(&config)`.
- **2026-04-18 — More indicators:** WMA, ROC(10), MFI(14), Keltner(20/14/2), Donchian(20), Aroon(25), Hull(9), Ultimate Oscillator, TSI(25,13); fields on `IndicatorSnapshot` sub-structs; still not used by default strategy gates.
- **2026-04-18 — Full missing-TV plan:** See [`context/indicator-roadmap.md`](indicator-roadmap.md) (tiers 1–9: A/D, CMF, volume MA/EMA, BB%B/bandwidth, Stoch RSI, AO, PSAR, SuperTrend, VWAP+anchor, Ichimoku, pivots, DEMA/TEMA, etc.).
- **2026-04-18 — Roadmap implemented:** Tiers 1–9 in `src/indicators/*.rs`; `IndicatorSnapshot` + `prepare.rs` wiring; `StrategyConfig` gained `vwap_anchor_mode` (`UtcDay` / `RollingBars` / `Disabled`), `vwap_rolling_bars`, `vwap_include_current_bar` (serde-defaulted). `cargo test` green; default strategy unchanged.
- **2026-04-18 — TA parity batch:** Added CMO, TRIX, KVO+signal (pandas-ta style SV), Chaikin osc on A/D, PVO, Force Index, NVI/PVI, Vortex, TTM Squeeze+LR momentum, Chandelier exit, KAMA/ALMA/VIDYA, MAMA+FAMA (Lean port), LR slope, price z-score, hist vol of log returns, candlestick pattern flags; `patterns` on `IndicatorSnapshot`.
- **2026-04-18 — `src/statistics/`:** Classical stats separate from TA (`descriptive`, `rank`, `association`, `shape`, `regression`); `pub mod statistics` in `lib.rs`.
